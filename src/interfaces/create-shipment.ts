export enum FragilityLevel {
    NONE = 0,
    LOW = 1,
    MEDIUM = 2,
    HIGH = 3,
}

export enum HandlingCategory {
    STANDARD = "standard",
    FRAGILE = "fragile",
    HAZARDOUS = "hazardous",
    PERISHABLE = "perishable",
}
